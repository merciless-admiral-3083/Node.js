const secret="don't share" //it wont be shared

const share1="Earth is round"
const share2="Rich are dry fruits"



const hello=(name) => { // we defined hello to contain a name and you have to print that name
    console.log(`Hello ${name}`) //here we will print Hello and any name assigned
}
module.exports ={share1, share2, hello} //We can now share this thing amongst many files, now go to JAS_1
